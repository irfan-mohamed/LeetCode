-- Write your PostgreSQL query statement below
WITH count_num AS(
    SELECT
        num,
        COUNT(num) AS num_cnt
    FROM
        MyNumbers
    GROUP BY
        num
)
SELECT
    MAX(num) AS num
FROM
    count_num
WHERE
    num_cnt = 1;
